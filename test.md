请根据所提供的数组大小，分析并修改以下代码中循环边界的取值，要求：
1. 只修改循环边界代码，不要引入任何其他函数，也不要改变代码其他部分内容，例如不要修改数组下标
2. 边界计算过程中，尽量增加循环的遍历次数，但不要导致数组越界！！！
5. 请只使用“+,-,*,/,sqrt”运算（sqrt最多使用一次），10以内的自然数以及所提供的参数（必须且只能使用其中一个）构造一个尽可能简洁的边界取值
6. 对于可能的死代码，保留但加上注释“dead code”说明
7. 最终修改的代码单独放在markdown代码块中给我

示例：
循环参数M=60, N=80，数组A[N]
代码为
for (int i = 0; i <M; i++) A[i*i]=i;
根据A[i*i]求得的边界为0<=i<=min(sqrt(N),M)，那么可能的最大边界为sqrt(N)，需将其放缩取整为8，并最终将其替换为0<=i<=M/10+2(或者N/10)。

学习我的上述示例并根据以下信息修改代码：
我的参数和数组大小：
{"PB_M": 60, "PB_L": 60, "PB_P": 60}
["A[PB_M,PB_L]", "B[PB_L,PB_P]"]

我的代码：
    for (int i = PB_L - 1; i >= 0; i--) {
        for (int j = 0; j < PB_L; j++) {
            if (j > 10) {
                A[i + j + i*j][i*i] = A[i + j + i*j][i*i] + A[i][j*j] * A[j][j + j*j] * 6;
            }
        }
        for (int k = 0; k < i; k++) {
            B[i][i + k + k*k] = 1;
        }
    }
    for (int l = 0; l < PB_L; l++) {
        for (int m = 0; m < PB_L; m++) {
            A[m + m*m][m] = 4;
        }
    }